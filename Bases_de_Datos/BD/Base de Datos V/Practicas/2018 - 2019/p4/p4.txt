1-
SET SERVEROUTPUT ON;
DECLARE
  V_NOMBRE VOTANTES.NOMBRECOMPLETO%TYPE;
  V_CORREO VOTANTES.EMAIL%TYPE;
  MYDNI NUMBER := 30983712;
BEGIN
  SELECT NOMBRECOMPLETO, EMAIL INTO V_NOMBRE, V_CORREO FROM VOTANTES WHERE DNI=MYDNI;
  DBMS_OUTPUT.PUT_LINE(V_NOMBRE||': '|| V_CORREO);
END;

Procedimiento PL/SQL terminado correctamente.
Jose Perez Perez: jpp@gmail.com

2-
SET SERVEROUTPUT ON;
DECLARE
  V_NOMBRE VOTANTES.NOMBRECOMPLETO%TYPE;
  V_CORREO VOTANTES.EMAIL%TYPE;
  MYDNI NUMBER := 30983712;
BEGIN
  SELECT SUBSTR(NOMBRECOMPLETO,1,5) INTO V_NOMBRE FROM VOTANTES WHERE DNI=MYDNI;
  SELECT  EMAIL INTO  V_CORREO FROM VOTANTES WHERE DNI=MYDNI;
  DBMS_OUTPUT.PUT_LINE(V_NOMBRE||': '|| V_CORREO);
END;

Procedimiento PL/SQL terminado correctamente.
Jose : jpp@gmail.com

3-
SET SERVEROUTPUT ON;
DECLARE
  V_NOMBRE VOTANTES.NOMBRECOMPLETO%TYPE;
  V_CORREO VOTANTES.EMAIL%TYPE;
  MYDNI NUMBER := 30983712;
BEGIN
  SELECT 'Pepe' || SUBSTR(NOMBRECOMPLETO,5,16) INTO V_NOMBRE FROM VOTANTES WHERE DNI=MYDNI;
  SELECT  EMAIL INTO  V_CORREO FROM VOTANTES WHERE DNI=MYDNI;
  DBMS_OUTPUT.PUT_LINE(V_NOMBRE||': '|| V_CORREO);
END;

Procedimiento PL/SQL terminado correctamente.
Pepe Perez Perez: jpp@gmail.com